alias g="git"

alias ll="ls -l"
alias la="ls -a"
alias lla="ls -la"
alias lll="ls -l | less"

alias zshconfig="subl ~/.zshrc"
alias dotsconfig="subl ~/.dotfiles"
alias ohmyzsh="subl ~/.oh-my-zsh"
alias zshreload="source ~/.zshrc"

# adding some sugar to git with hub
eval "$(hub alias -s)"
